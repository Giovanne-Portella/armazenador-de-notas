[build]
  publish = "."
  functions = "netlify/functions"

# Scheduled function — roda a cada minuto para verificar lembretes
# Netlify suporta cron scheduling nativo
[[headers]]
  for = "/sw.js"
  [headers.values]
    Service-Worker-Allowed = "/"
    Cache-Control = "no-cache"
